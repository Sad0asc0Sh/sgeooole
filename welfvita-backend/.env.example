# ═══════════════════════════════════════════════════════════════════════════════
# 🔐 WELFVITA BACKEND - Environment Variables
# ═══════════════════════════════════════════════════════════════════════════════
# این فایل نمونه است. کپی کنید به .env و مقادیر را تنظیم کنید:
# cp .env.example .env
# ═══════════════════════════════════════════════════════════════════════════════

# =============================================================================
# 🗄️ DATABASE
# =============================================================================
MONGODB_URI=mongodb://localhost:27017/welfvita

# =============================================================================
# 🔑 JWT & AUTHENTICATION
# =============================================================================
# توکن JWT باید یک رشته تصادفی قوی باشد (حداقل 32 کاراکتر)
# می‌توانید با این دستور تولید کنید: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING
JWT_EXPIRES_IN=7d

# =============================================================================
# 👤 ADMIN SETUP (/api/admin/setup)
# =============================================================================
# 🔒 روش جدید و امن برای ایجاد ادمین اولیه (توصیه می‌شود)
# به جای seedAdmin.js از endpoint /api/admin/setup استفاده کنید
#
# کلید امنیتی Setup - این را به یک رشته تصادفی تغییر دهید:
# می‌توانید با این دستور تولید کنید: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SETUP_KEY=CHANGE_THIS_TO_A_SECURE_RANDOM_KEY

# تنظیمات اختیاری ادمین (اگر تنظیم نشود، مقادیر پیش‌فرض استفاده می‌شود)
# ADMIN_EMAIL=admin@welfvita.com
# ADMIN_NAME=مدیر سیستم
#
# ⚠️ هشدار: اگر ADMIN_PASSWORD تنظیم کنید، باید حداقل 12 کاراکتر باشد
# و شامل حروف بزرگ، کوچک، عدد و کاراکتر خاص باشد
# اگر تنظیم نشود، پسورد امن به صورت خودکار تولید می‌شود
# ADMIN_PASSWORD=YourSecurePassword123!

# =============================================================================
# 👥 TEST USER SEED (seedUser.js)  
# =============================================================================
# TEST_USER_EMAIL=testuser@welfvita.com
# TEST_USER_PASSWORD=SecureTestPass123!

# =============================================================================
# 📧 EMAIL (برای بازنشانی رمز عبور)
# =============================================================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password
FROM_EMAIL=noreply@welfvita.com

# =============================================================================
# ☁️ CLOUDINARY (آپلود تصاویر)
# =============================================================================
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# =============================================================================
# 🌐 APPLICATION
# =============================================================================
NODE_ENV=development
PORT=5000
FRONTEND_URL=http://localhost:3000
ADMIN_URL=http://localhost:3001
